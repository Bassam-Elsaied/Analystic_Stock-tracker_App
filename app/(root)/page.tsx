/**
 * Root page component that renders the home wrapper container.
 *
 * @returns The top-level JSX element for the home page: a `div` with `className` "flex min-h-screen home-wrapper".
 */
export default function Home() {
  return <div className="flex min-h-screen home-wrapper"></div>;
}
